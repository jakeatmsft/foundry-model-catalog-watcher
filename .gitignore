# Baseline state — machine/tenant specific, don't commit unless you intend it as an audit trail
model-watch-state.json

# Local output
*.csv
*.log

# Saved filters and alert secrets
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.py[cod]
